// views/work_view.dart
import 'package:flutter/material.dart';

class WorkView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Work View')),
      body: Center(
        child: Text('This is the Work View'),
      ),
    );
  }
}
